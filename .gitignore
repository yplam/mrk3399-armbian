# armbian/build clone, created by `make setup`
/build/
# template armbian/build copies into userpatches when it is missing.
# customize-image.sh is NOT ignored: this port ships its own (it installs the
# USB gadget), and Armbian only copies its template in when the file is absent.
/userpatches/config-example.conf
